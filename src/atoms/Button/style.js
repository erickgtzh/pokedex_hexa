// src/components/atoms/Button/styles.ts
import {StyleSheet} from 'react-native';
import {colors} from '../../colors';

const styles = StyleSheet.create({
  button: {
    backgroundColor: colors.primary,
  },
});

export default styles;
