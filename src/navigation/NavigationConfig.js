import React from 'react';
import { NavigationContainer } from '@react-navigation/native';

const NavigationConfig = () => {
  return (
    <NavigationContainer>
      {/* TODO: Definir la navegación */}
    </NavigationContainer>
  );
};

export default NavigationConfig;
